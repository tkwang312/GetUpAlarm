import SwiftUI

struct ViewD: View {
    var body: some View {
        ZStack{
            Color.white
            Text("Settings").foregroundColor(Color.black)
        }    }
}

#Preview {
    ViewD()
}
